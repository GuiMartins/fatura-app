<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- AppCompatActivity requires a Theme.AppCompat descendant - only affects
         window-level attributes before Compose's setContent takes over, all
         actual UI/colors come from the CassholeTheme composable (Material3). -->
    <style name="Theme.Casshole" parent="Theme.AppCompat.DayNight.NoActionBar" />
</resources>
